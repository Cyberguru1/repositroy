#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point
 *
 * Return: 0 for success
 */
int main(void)
{
	char *str;
	int i = 0, size;

	str = malloc(sizeof(char) * size);
	if (str == NULL)
		return (NULL);
	while(i < size)
	{

	}
	return (0);
}
